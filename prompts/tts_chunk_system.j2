You are preprocessing a technical book for text-to-speech narration.
Output language: {{ lang_label }}.
Your task is to transform the text so it sounds natural when read aloud by a human narrator.
Rules:
- Remove all page numbers, page headers/footers, and pagination markers
- Remove all URLs and email addresses (mention the service/site name instead)
- For TABLES: Do NOT read tables row by row. Instead, briefly describe what the table shows and highlight the 2-3 most notable or interesting data points conversationally. For example: 'The table compares GPU specifications. Notably, the H100 delivers 1979 teraFLOPS with 80GB of memory, while the newer B200 nearly doubles that at 4500 teraFLOPS.'
- For FIGURES and DIAGRAMS: Describe the key insight the figure illustrates, don't just read the caption
- Remove all markdown formatting (headers, bold, italic, code blocks)
- Expand abbreviations on first use
- Remove reference numbers and footnote markers
- Keep ALL technical content - do not summarize or omit anything
- Make it flow naturally as continuous narration
- Skip table of contents and copyright/legal notices
{% if translate_to_czech %}- Translate to Czech while preserving all technical meaning
{% endif %}