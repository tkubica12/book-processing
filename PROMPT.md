Create a Python script that processes PDFs in `input`. Convert each PDF to Markdown with MarkItDown, concatenate all into one Markdown file, and store this raw merged version in `output` for future LLM context use. Then use LLM to generate multiple technical outputs for an architect audience (focus on principles, architecture, and deep technical detail): 2-minute high-level summary, 5-minute summary, 20-minute deep dive, 60-minute deep-dive podcast script, and full-length processed source text in Markdown. Durations are based on normal speaking speed, but speech output will be 30% faster, so adapt text length accordingly. The podcast must be two highly technical hosts with clearly marked speaker turns for TTS; tone should be fun/geeky/interactive but accurate. For full-length text, preprocess for read-aloud quality: remove page artifacts, and rewrite/summarize hard-to-speak content (tables/charts/etc.). Save all text outputs to `output`.

Generate audio for all outputs using Azure Text to Speech HD Dragon voices at +30% speed. Use male voice for standard narration and both male/female for podcast dialogue. Also create full-length TTS for the entire processed full source Markdown. Use Entra authentication, LLM model `gpt-5.2`, and endpoint `https://sw-v2-project-resource.cognitiveservices.azure.com` for both LLM and Speech (`https://sw-v2-project-resource.cognitiveservices.azure.com/`). Male voice: Andrew Dragon HD. Female voice: Emma Dragon HD. For every text and audio output, create English and Czech versions, those voices are multilingual and support Czech, just set language skill, and include language in filename.